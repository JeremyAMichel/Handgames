<div class="carousel-item">
    <a href="{{ game.path }}"><div class="carousel-img"><img src="{{ game.thumbnail }}" width="100%" height="100%" class="d-block w-100" alt="..."></div>
        <div id="mediaResponsive" class="carousel-caption d-none d-md-block">
            <h5>{{ game.nom }}</h5>
            <p>{{ game.description }}</p>
        </div>
    </a>
</div>